Auto Scroll, a hands-free mode that scrolls the page for you at an adjustable pace, use key2 to open a menu adjuste the pace of the scroll. 5 tipes of speed if possible 
adicionar um menu de configuração similar ao do webserver 
usar a key2 para standby

=== v1.8.0 — a verificar no dispositivo (nao coberto pelos testes de host) ===
Atomicidade do .tmp + rename do reader_progress.json (cortar energia a meio de um virar de pagina).
Tecto de 64 KB no POST /api/library/import (deve responder 413, nao 500).
Poda no scanBooks(): apagar um .epub fora da web UI e confirmar que a entrada desaparece.
Importar o estado ANTES de enviar os .epub: as entradas ficam pending e sobrevivem ao scan seguinte.
Migracao v1->v2: gravar um reader_progress.json antigo (chaves com "/") e confirmar que as posicoes se mantem.

=== v1.14.0 — a verificar no dispositivo (nao coberto pelos testes de host) ===
(ver docs/plans/2026-08-29-bookmarks-and-goto-percent-design.md; pio run nao
correu neste ambiente, so os testes de host da logica pura)
Marcadores: adicionar, saltar e apagar via web UI; confirmar que "saltar" so
faz efeito ao reabrir o livro no dispositivo, nao no livro que possa estar
aberto nessa altura (nao devia estar, com o WiFi desligado durante a leitura).
Tecto de 20 marcadores por livro: confirmar resposta 409 do POST /api/bookmarks/add.
Ir para %: pedir 100% num livro grande e confirmar que o scan por capitulos
(orcamentado a TOTAL_PAGES_BUDGET_MS por tick) nao trava a UI nem atrasa
visivelmente a abertura do livro.
Ir para %: confirmar que nao ha duplo refresh e-ink (pagina restaurada seguida
da pagina alvo) ao abrir um livro com um pedido pendente.
Ir para %: premir um botao durante o scan (a janela costuma ser curta) e
confirmar que a entrada e mesmo ignorada em vez de agir sobre a posicao
restaurada antes do salto aterrar.
Ir para %: livro so com imagens (capitulos sem texto) cai no fallback
proporcional em vez de aterrar sempre no capitulo 0.

=== v1.15.0 — a verificar no dispositivo (nao coberto pelos testes de host) ===
(ver docs/plans/2026-08-29-pt-hyphenation-design.md)
Abrir um livro com palavras compostas longas (fonte "Large") e confirmar
visualmente que o corte aparece com hifen no fim da linha, nao a meio de um
digrafo (ch/lh/nh) nem a meio de um grupo consoante+liquida (br/tr/vr/...).
Confirmar que a posicao de leitura (pagina, capitulo) continua correcta
depois de uma palavra hifenizada — o hifen e so visual, nunca deve entrar na
contagem de paginas nem no ponto de retoma.
Confirmar que uma palavra sem hifenizacao valida (URL, numero, palavra com
hifen ja existente) continua a cortar como antes (por caracter, sem hifen),
sem regressao.

=== v1.16.0 — a verificar no dispositivo (nao coberto pelos testes de host) ===
(ver docs/plans/2026-08-29-gestao-biblioteca-e-estado-design.md; pio run nao
correu neste ambiente — api.registry.platformio.org bloqueado pela politica de
rede — so os testes de host da logica pura)

Relogio: ligar o WiFi e confirmar no monitor serie o "TimeMgr: SNTP started"
seguido de datas plausiveis na web UI. Depois entrar em standby (KEY2 longo),
acordar, e confirmar que a hora se mantem — o ESP-IDF deve preserva-la atraves
do deep sleep, e e disso que depende uma so sincronizacao por ciclo de energia.
Cortar a energia e reabrir a web UI ANTES de haver WiFi: as datas novas devem
ficar a "—", nunca uma data inventada, e clockSynced deve vir a false.

Override: marcar um livro como lido na web UI, abri-lo no dispositivo e virar
algumas paginas. Confirmar que a marca SOBREVIVE — e a armadilha do
ProgressStore::set(), que antes reconstruia a entrada inteira a cada virar de
pagina. Confirmar tambem que as datas nao se perdem no mesmo caminho.

Data de conclusao: ler um livro pequeno ate passar os 96% com o relogio
acertado e confirmar que finishedAt fica gravado uma unica vez — voltar atras e
avancar outra vez pelo ultimo capitulo nao deve mexer na data.

Poda: marcar um livro como lido, apaga-lo pela web UI e confirmar que a entrada
sobrevive ao scanBooks() seguinte (a lista de "livros que li" tem de aguentar
arrumar o dispositivo). Um livro NAO marcado deve continuar a ser podado como
antes.

Biblioteca do PC: escolher a pasta no Chrome e no Firefox e confirmar os tres
grupos. Enviar em lote com a particao quase cheia e confirmar que a verificacao
de espaco recusa ANTES de comecar, em vez de deixar uploads meio escritos.
Recarregar a pagina sem re-escolher a pasta: o diff deve continuar la, com a
data da listagem e os botoes Enviar desactivados.
Num browser movel (sem webkitdirectory) a seccao deve desaparecer, nao dar erro.

Ficheiro de estado: confirmar a migracao v2->v3 com um reader_progress.json
antigo (sem override nem datas) — as posicoes mantem-se e as entradas ficam
Auto. Exportar e importar entre dois estados com marcas diferentes e confirmar
a regra do startedAt mais antigo a ganhar.

Concorrencia: o PageCountStore passou a ter mutex porque o GET /api/books o le
a partir da tarefa do servidor. Confirmar que abrir a web UI enquanto uma
contagem de paginas esta a decorrer nao trava nem corrompe o total.

=== v1.17.0 — a verificar no dispositivo (nao coberto pelos testes de host) ===
(ver docs/plans/2026-08-29-titulo-do-livro-na-biblioteca-design.md; pio run nao
correu neste ambiente, so os testes de host da logica pura)
Primeira visita a biblioteca depois de actualizar: os titulos comecam pelos
nomes de ficheiro cortados e passam aos titulos do EPUB ao fim de algumas
passagens, com UM so refresh no fim do lote e nao um por livro.
Confirmar que os botoes continuam a responder enquanto os titulos sao lidos —
cada passagem abre um ZIP, e e a unica coisa nova no caminho do update().
Segunda visita (e reinicio): a lista abre ja com os titulos certos, sem
reabrir os EPUB (/book_titles.json no SystemFS ja os tem).
Titulo longo: confirmar as duas linhas, e que a segunda linha nao toca no
"pag. x/y" nem com o item seleccionado (era o caso das tres linhas antigas).
Titulo muito longo: confirmar as reticencias no fim da segunda linha, dentro
da caixa, sem transbordar para a margem.
EPUB sem <dc:title> ou com o OPF ilegivel: fica o nome do ficheiro e o livro
NAO volta a ser reaberto a cada passagem (ver no monitor serie que nao ha
actividade repetida de ZIP na biblioteca parada).
Apagar um livro pela web UI: a entrada correspondente sai do
/book_titles.json no scan seguinte (mesmo ponto onde as posicoes e os
marcadores sao podados).
Acentos: um titulo com caracteres portugueses (Ó, ç, ã) desenhado a partir do
<dc:title> em UTF-8 tem de aparecer certo depois da conversao para Latin-1.
